# =============================================================================
# HeyPiggy Vision Worker — Dev Dependencies (pinned)
# =============================================================================
#
# Source of truth: pyproject.toml ([project.optional-dependencies].dev).
# Install with:
#   pip install -r requirements.txt -r requirements-dev.txt
# -----------------------------------------------------------------------------

-r requirements.txt

# --- Lint / format ---------------------------------------------------------
ruff==0.15.11

# --- Type checking ---------------------------------------------------------
mypy==1.20.2
types-Pillow==10.2.0.20240822

# --- Security --------------------------------------------------------------
bandit[toml]==1.9.4
detect-secrets==1.5.0
pip-audit==2.10.0

# --- Pre-commit ------------------------------------------------------------
pre-commit==4.6.0

# --- Testing ---------------------------------------------------------------
pytest==9.0.3
pytest-asyncio==1.3.0
pytest-cov==7.1.0
pytest-mock==3.15.1
