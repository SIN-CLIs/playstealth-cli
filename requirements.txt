# =============================================================================
# HeyPiggy Vision Worker — Runtime Dependencies (pinned)
# =============================================================================
#
# Source of truth: pyproject.toml.
# This file exists for pip-based deployments (Docker, CI) that want a
# reproducible, pinned install without reading PEP 621 metadata.
#
# Keep in sync with [project.dependencies] in pyproject.toml.
#
# Install:
#   pip install -r requirements.txt
# -----------------------------------------------------------------------------

# --- Core -------------------------------------------------------------------

# Image downscaling for NVIDIA NIM inline uploads (hard ~180 KB limit).
# Without Pillow the vision path produces HTTP 400 on large screenshots.
Pillow==11.0.0

# Structured JSON logging with contextvars + secret redaction.
# Replaces print() everywhere in the worker/ package.
structlog==24.4.0
